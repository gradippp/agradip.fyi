# SteganoLSB

A command-line tool for image steganography.

## Intention

To explore low-level bit manipulation and learn more about how image data is structured by hiding secret messages within pixels.

## How it works

It uses the Least Significant Bit (LSB) technique to embed message data into the RGB values of a bitmap image without changing the visual appearance significantly.
